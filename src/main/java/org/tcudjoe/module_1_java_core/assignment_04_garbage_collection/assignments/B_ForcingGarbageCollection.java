package org.tcudjoe.module_1_java_core.assignment_04_garbage_collection.assignments;

/**
 * Forcing Garbage Collection (For Experiment)
 * <p>
 * - Create a program that allocates large objects.
 * - Suggest GC with `System.gc()` (though it's just a suggestion).
 * - Monitor memory usage before and after GC.
 */
public class B_ForcingGarbageCollection {
}
